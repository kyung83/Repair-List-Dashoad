"use client";
import type {ActionItem,Part,PlannedPart} from './maintenance-types';

type Props={eventType:'pm'|'annual';canWork:boolean;busy:boolean;finding:string;setFinding:(v:string)=>void;items:ActionItem[];parts:Part[];partIds:Record<number,string>;partQty:Record<number,number>;setPartId:(id:number,v:string)=>void;setPartQty:(id:number,v:number)=>void;create:(kind:'now'|'next')=>void;plan:(item:ActionItem)=>void;removePart:(item:ActionItem,part:PlannedPart)=>void;cancel:(item:ActionItem)=>void};
const n=(v:number)=>Number.isInteger(v)?String(v):v.toFixed(2).replace(/0+$/,'').replace(/\.$/,'');

export default function MaintenanceFinalActions(p:Props){const next=p.eventType==='annual'?'Next Annual':'Next PM';return <section style={{marginTop:18,border:'2px solid #f2c66d',borderRadius:14,padding:16,background:'#fffaf0'}}>
 <p className="easy-eyebrow">FINAL CHECK</p><h4 style={{margin:'6px 0',fontSize:20,color:'#17283a'}}>Anything else you noticed on this unit?</h4><p style={{margin:'0 0 12px',color:'#657383',lineHeight:1.5}}>This is for extra findings that are not failed checklist items. Choose whether it must be repaired before this visit closes or should follow the unit to the {next}.</p>
 {p.canWork&&<><textarea className="easy-note" value={p.finding} onChange={e=>p.setFinding(e.target.value)} placeholder="Example: Right mirror is getting loose; replace bracket."/><div className="easy-actions" style={{marginTop:10}}><button className="easy-button danger" disabled={p.busy} onClick={()=>p.create('now')}>Repair Before Closing</button><button className="easy-button orange" disabled={p.busy} onClick={()=>p.create('next')}>Save for {next}</button></div></>}
 {p.items.length>0&&<div style={{marginTop:15,display:'grid',gap:10}}>{p.items.map(item=>{const open=!item.repairStatus.toLowerCase().includes('complete');return <article key={item.id} style={{background:'white',border:'1px solid #e3d4b3',borderRadius:10,padding:12}}>
  <div style={{display:'flex',justifyContent:'space-between',gap:12,alignItems:'flex-start',flexWrap:'wrap'}}><div><strong style={{display:'block',fontSize:15,color:'#243543'}}>{item.description}</strong><span style={{display:'block',marginTop:3,fontSize:12,color:'#6b7784'}}>{item.disposition==='now'?'Required before this visit closes':`Waiting for ${next}`} · Repair {item.repairId.replace('repair-','#')}</span></div><span className={`easy-badge ${item.disposition==='now'&&open?'red':'orange'}`}>{item.disposition==='now'?(open?'REPAIR NOW':'COMPLETED'):next.toUpperCase()}</span></div>
  <div className="easy-actions" style={{marginTop:9}}>{item.disposition==='now'&&open&&<a className="easy-button danger" href={`/shop?repairId=${encodeURIComponent(item.repairId)}`}>Open Repair Job</a>}{item.disposition==='next'&&open&&p.canWork&&<button className="easy-button" disabled={p.busy} onClick={()=>p.cancel(item)}>Remove from {next}</button>}</div>
  {item.plannedParts.length>0&&<div style={{marginTop:10}}><strong style={{fontSize:12,color:'#52616e'}}>Planned parts</strong><div style={{marginTop:5,display:'grid',gap:5}}>{item.plannedParts.map(part=><div key={part.id} style={{display:'flex',justifyContent:'space-between',gap:8,alignItems:'center',fontSize:12,padding:'6px 8px',borderRadius:7,background:'#f7f9fa'}}><span><b>{part.partNumber}</b> × {n(part.quantity)} · {part.description}</span>{p.canWork&&part.usedQuantity<=0&&open&&<button className="easy-button" style={{minHeight:32,padding:'0 8px'}} disabled={p.busy} onClick={()=>p.removePart(item,part)}>Remove</button>}</div>)}</div></div>}
  {p.canWork&&open&&p.parts.length>0&&<div style={{marginTop:10,display:'grid',gridTemplateColumns:'minmax(180px,1fr) 80px auto',gap:7}}><select className="easy-search-input" value={p.partIds[item.id]??''} onChange={e=>p.setPartId(item.id,e.target.value)} disabled={p.busy}><option value="">Plan a part without using inventory</option>{p.parts.map(part=><option key={part.id} value={part.id}>{part.partNumber} — {part.description} ({n(part.quantityOnHand)} on hand)</option>)}</select><input className="easy-search-input" type="number" min="0.01" step="any" value={p.partQty[item.id]??1} onChange={e=>p.setPartQty(item.id,Number(e.target.value))} disabled={p.busy}/><button className="easy-button" disabled={p.busy} onClick={()=>p.plan(item)}>Attach Part</button></div>}
 </article>;})}</div>}
 </section>}
