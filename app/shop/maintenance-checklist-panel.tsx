"use client";
import {useEffect,useState} from 'react';
import InspectionQuestion from './inspection-question';
import MaintenanceFinalActions from './maintenance-final-actions';
import type {ActionData,ActionItem,ChecklistData,ChecklistItem,Part,PlannedPart} from './maintenance-types';

type Props={repairId:string;canWork:boolean;parts?:Part[]};
const mileage=(v:number|null|undefined)=>v==null?'Not available':`${Number(v).toLocaleString()} mi`;

export default function MaintenanceChecklistPanel({repairId,canWork,parts=[]}:Props){
 const[data,setData]=useState<ChecklistData|null>(null),[actions,setActions]=useState<ActionData|null>(null),[unavailable,setUnavailable]=useState(false),[busy,setBusy]=useState(false),[message,setMessage]=useState(''),[notes,setNotes]=useState<Record<number,string>>({}),[manualMileage,setManualMileage]=useState(''),[currentNumber,setCurrentNumber]=useState<number|null>(null),[finding,setFinding]=useState(''),[inventoryParts,setInventoryParts]=useState<Part[]>(parts),[partIds,setPartIds]=useState<Record<number,string>>({}),[partQty,setPartQty]=useState<Record<number,number>>({});
 function accept(p:ChecklistData){setData(p);setNotes(Object.fromEntries(p.items.map(i=>[i.number,i.notes||''])));if(p.mileageSource==='Manual'&&p.currentMileage!=null)setManualMileage(String(p.currentMileage));setCurrentNumber(c=>c&&p.items.some(i=>i.number===c)?c:p.items.find(i=>i.result==='pending')?.number??p.items.find(i=>i.result==='fail')?.number??p.items[0]?.number??null)}
 async function loadActions(){const r=await fetch(`/api/maintenance-actions?repairId=${encodeURIComponent(repairId)}`,{cache:'no-store'}),p=await r.json() as ActionData&{error?:string};if(!r.ok)throw new Error(p.error||'Maintenance action items could not be loaded.');setActions(p)}
 async function load(){const r=await fetch(`/api/maintenance-checklist?repairId=${encodeURIComponent(repairId)}`,{cache:'no-store'}),p=await r.json() as ChecklistData&{error?:string};if(!r.ok){if((p.error||'').includes('only available for scheduled PM and annual')){setUnavailable(true);setData(null);return}throw new Error(p.error||'Maintenance checklist could not be loaded.')}setUnavailable(false);accept(p);await loadActions();if(parts.length)setInventoryParts(parts);else{const s=await fetch('/api/shop',{cache:'no-store'});if(s.ok)setInventoryParts(((await s.json()) as{parts?:Part[]}).parts??[])}}
 useEffect(()=>{setData(null);setActions(null);setUnavailable(false);setMessage('');setCurrentNumber(null);setFinding('');void load().catch(e=>setMessage(e instanceof Error?e.message:'Maintenance checklist could not be loaded.'))},[repairId]);
 async function checklist(body:Record<string,unknown>){setBusy(true);setMessage('');try{const r=await fetch('/api/maintenance-checklist',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({...body,repairId})}),p=await r.json() as ChecklistData&{ok?:boolean;error?:string};if(!r.ok||!p.ok)throw new Error(p.error||'Checklist change failed.');accept(p);await loadActions().catch(()=>undefined);return p}catch(e){setMessage(e instanceof Error?e.message:'Checklist change failed.');return null}finally{setBusy(false)}}
 async function action(body:Record<string,unknown>,success:string){setBusy(true);setMessage('');try{const r=await fetch('/api/maintenance-actions',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({...body,repairId})}),p=await r.json() as ActionData&{ok?:boolean;error?:string};if(!r.ok||!p.ok)throw new Error(p.error||'Maintenance action failed.');setActions(p);setMessage(success);return p}catch(e){setMessage(e instanceof Error?e.message:'Maintenance action failed.');return null}finally{setBusy(false)}}
 function next(p:ChecklistData,n:number){const x=p.items.findIndex(i=>i.number===n);return p.items.slice(x+1).find(i=>i.result==='pending'||i.result==='fail')?.number??p.items.slice(0,x).find(i=>i.result==='pending'||i.result==='fail')?.number??n}
 async function setItem(item:ChecklistItem,result:ChecklistItem['result']=item.result){const note=(notes[item.number]??'').trim();if(result==='fail'&&!note){setMessage('Describe what is wrong in the note box, then press FAIL again.');return}const p=await checklist({action:'setItem',itemNumber:item.number,result,notes:note});if(!p)return;if(result==='fail'){setCurrentNumber(item.number);setMessage('Repair created. Fix this item before finishing the inspection. You can add parts to the repair job.')}else{setCurrentNumber(next(p,item.number));setMessage(result==='pass'&&item.result==='fail'?'Repair marked corrected and the checklist item now passes.':'Saved.')}}
 async function photo(item:ChecklistItem,file:File|null){if(!file)return;setBusy(true);setMessage('');try{const f=new FormData();f.set('action','uploadPhoto');f.set('repairId',repairId);f.set('itemNumber',String(item.number));f.set('photo',file);const r=await fetch('/api/maintenance-checklist',{method:'POST',body:f}),p=await r.json() as ChecklistData&{ok?:boolean;error?:string};if(!r.ok||!p.ok)throw new Error(p.error||'Photo upload failed.');accept(p);setMessage('Photo saved.')}catch(e){setMessage(e instanceof Error?e.message:'Photo upload failed.')}finally{setBusy(false)}}
 async function create(kind:'now'|'next'){const description=finding.trim();if(!description){setMessage('Describe what you found first.');return}const label=data?.eventType==='annual'?'Annual':'PM',p=await action({action:'createActionItem',description,disposition:kind},kind==='now'?`Repair created for this ${label}. It must be completed before closing.`:`Saved as a real repair for the next ${label}. You can plan parts now.`);if(p)setFinding('')}
 async function plan(item:ActionItem){const id=partIds[item.id]??'',q=partQty[item.id]??1;if(!id){setMessage('Choose a part to plan for this repair.');return}if(!Number.isFinite(q)||q<=0){setMessage('Enter a positive planned quantity.');return}if(await action({action:'planPart',actionItemId:item.id,partId:Number(id),quantity:q},'Planned part attached to the repair. Inventory was not consumed.')){setPartIds(v=>({...v,[item.id]:''}));setPartQty(v=>({...v,[item.id]:1}))}}
 async function removePart(item:ActionItem,p:PlannedPart){await action({action:'removePlannedPart',actionItemId:item.id,plannedPartId:p.id},`${p.partNumber} removed from the plan.`)}
 async function cancel(item:ActionItem){if(window.confirm(`Remove this item from the next ${item.targetEventType==='annual'?'Annual':'PM'}?`))await action({action:'cancelFutureAction',actionItemId:item.id},'Future service item removed.')}
 if(unavailable||(!data&&!message))return null;if(!data)return <div className="easy-notice">{message||'Loading inspection…'}</div>;
 const done=data.items.filter(i=>i.result!=='pending').length,pending=data.pendingCount??data.items.filter(i=>i.result==='pending').length,failed=data.failedCount??data.items.filter(i=>i.result==='fail').length,ready=data.status==='ready',completed=data.status==='completed',current=data.items.find(i=>i.number===currentNumber)??data.items.find(i=>i.result==='pending')??data.items.find(i=>i.result==='fail')??data.items[0],index=Math.max(0,data.items.findIndex(i=>i.number===current?.number)),pct=data.items.length?Math.round(done/data.items.length*100):0,required=actions?.requiredRepairs??[],finalStep=data.started&&pending===0&&failed===0&&!completed;
 return <section className="easy-checklist">
  <div style={{display:'flex',justifyContent:'space-between',gap:16,alignItems:'flex-start',flexWrap:'wrap'}}><div><p className="easy-eyebrow">STEP-BY-STEP INSPECTION</p><h3 className="easy-section-title" style={{fontSize:26,marginTop:6}}>{data.eventType==='annual'?'Annual Inspection':'PM Inspection'} — Unit {data.unit||'—'}</h3><p className="easy-section-copy">Do one item at a time. The system saves the record as you go.</p></div><div className="easy-badge green" style={{fontSize:13,minHeight:36}}>{mileage(data.currentMileage)} · {data.mileageSource==='Geotab'?'Automatic':'Manual'}</div></div>
  {message&&<div className="easy-notice">{message}</div>}
  {!data.started?<div className="easy-finish" style={{marginTop:16}}><strong style={{display:'block',fontSize:18}}>Ready to begin?</strong><p style={{margin:'6px 0 0'}}>Starting saves the mileage and creates the permanent inspection record.</p>{canWork&&<button className="easy-button orange" style={{marginTop:12}} disabled={busy} onClick={()=>void checklist({action:'startChecklist'})}>Start {data.eventType==='annual'?'Annual':'PM'}</button>}</div>:<>
   <div style={{marginTop:16,display:'flex',justifyContent:'space-between',fontSize:13,fontWeight:850,color:'#52616e'}}><span>{done} of {data.items.length} checked</span><span>{pct}%</span></div><div className="easy-progress-track" style={{marginTop:7}}><div className="easy-progress-fill" style={{width:`${pct}%`}}/></div>
   {current&&<InspectionQuestion item={current} note={notes[current.number]??''} busy={busy} canWork={canWork} completed={completed} eventType={data.eventType} onNote={v=>setNotes(n=>({...n,[current.number]:v}))} onResult={(i,r)=>void setItem(i,r)} onSave={i=>void setItem(i)} onPhoto={(i,f)=>void photo(i,f)} onRemovePhoto={id=>void checklist({action:'removePhoto',photoId:id})}/>} 
   <div className="easy-check-nav"><button className="easy-button" disabled={index<=0} onClick={()=>setCurrentNumber(data.items[index-1]?.number??null)}>← Previous</button><span style={{color:'#6b7784',fontSize:13}}>Item {index+1} of {data.items.length}</span><button className="easy-button" disabled={index>=data.items.length-1} onClick={()=>setCurrentNumber(data.items[index+1]?.number??null)}>Next →</button></div><details className="easy-review"><summary>Review all checklist answers</summary><div className="easy-chip-grid">{data.items.map(i=><button key={i.number} className={`easy-chip ${i.result}`} onClick={()=>setCurrentNumber(i.number)}>{i.number}</button>)}</div></details>
   {data.mileageSource==='Manual'&&data.eventType==='pm'&&!completed&&<div style={{marginTop:16,maxWidth:360}}><label style={{display:'grid',gap:6,fontWeight:900,color:'#52616e',fontSize:13}}>Current mileage<input className="easy-search-input" type="number" min="0" step="1" value={manualMileage} onChange={e=>setManualMileage(e.target.value)} disabled={!canWork||busy}/></label></div>}
   {finalStep&&!ready&&<MaintenanceFinalActions eventType={data.eventType} canWork={canWork} busy={busy} finding={finding} setFinding={setFinding} items={actions?.actionItems??[]} parts={inventoryParts} partIds={partIds} partQty={partQty} setPartId={(id,v)=>setPartIds(x=>({...x,[id]:v}))} setPartQty={(id,v)=>setPartQty(x=>({...x,[id]:v}))} create={k=>void create(k)} plan={i=>void plan(i)} removePart={(i,p)=>void removePart(i,p)} cancel={i=>void cancel(i)}/>} 
   {!completed&&!ready&&<div className={`easy-finish ${failed||required.length?'blocked':''}`}>{pending?<><strong>{pending} inspection item{pending===1?'':'s'} still need an answer.</strong><p>Finish those before closing the job.</p></>:failed?<><strong>{failed} failed item{failed===1?'':'s'} still need repair.</strong><p>Fix each failed item and change it to Pass.</p></>:required.length?<><strong>{required.length} repair{required.length===1?'':'s'} must be completed before this {data.eventType==='annual'?'Annual':'PM'} can finish.</strong><div style={{marginTop:9,display:'grid',gap:6}}>{required.map(r=><div key={r.repairId} className="easy-row"><span><b>{r.title}</b><small style={{display:'block'}}>{r.status}</small></span><a className="easy-button danger" href={`/shop?repairId=${encodeURIComponent(r.repairId)}`}>Open Repair</a></div>)}</div></>:<><strong>Inspection is complete.</strong><p>Future-service items do not block this visit.</p>{canWork&&<button className="easy-button primary" disabled={busy} onClick={()=>void checklist({action:'markReady',...(data.mileageSource==='Manual'&&data.eventType==='pm'?{mileage:manualMileage}:{})})}>Finish Inspection</button>}</>}</div>}
   {ready&&<div className="easy-finish"><strong>Inspection complete.</strong><p>Complete the job in Shop Jobs. Maintenance history and the next schedule update automatically.</p></div>}{completed&&<div className="easy-finish"><strong>Maintenance completed.</strong>{data.eventType==='annual'&&<div className="easy-actions"><a className="easy-button orange" href={`/annual-inspections/print?repairId=${encodeURIComponent(data.repairId)}`}>Print Annual Form</a></div>}</div>}
  </>}
 </section>
}
