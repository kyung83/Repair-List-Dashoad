# Work order printing

The `/work-orders/print` view prints one completed work order at a time on a US Letter page using the full printable page area. Screen-only search and selection controls are hidden automatically during printing.
